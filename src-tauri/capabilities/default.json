{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window capabilities for the Family Archive main window. No network, filesystem or shell permissions are granted to the webview — all privileged work happens in Rust commands.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:app:allow-version"
  ]
}
